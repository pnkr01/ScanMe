def spacer(my_wind,tk):
    space_label = tk.Label(my_wind, height=1)
    space_label.pack()
def label_wind(my_wind,tk):
    label1 = tk.Label(my_wind, text="Upload an Image", width=30, font=("times", 18, "bold"), pady=10)
    label1.pack()